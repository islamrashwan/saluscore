# Core app & UI
streamlit==1.28.2          # app UI (used in app.py)
matplotlib==3.8.0          # plotting for SHAP bar chart

# Data & utilities
pandas==2.2.0              # DataFrame IO/manipulation
numpy==2.0.0              # numerical backbone
joblib==1.2.0              # load/save model & pipeline artifacts
openpyxl==3.1.2            # Excel reading (pandas read_excel engine)

# ML stack
scikit-learn==1.6.1        # Pipeline, IterativeImputer (RF), Bagging
xgboost==2.1.4             # base learners in your BaggingClassifier
shap==0.42.1               # per-patient explanations
